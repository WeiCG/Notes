# 第三章课后习题

> 以下答案全部省略main函数和导入的默认包

- 3.1

```java
System.out.print("Enter a, b, c: ");
Scanner sc = new Scanner(System.in);
float a = sc.nextFloat();
float b = sc.nextFloat();
float c = sc.nextFloat();

float discriminant = b * b - 4 * a * c;

if(discriminant > 0) {
	float r1 = (float)(Math.sqrt(discriminant) - b) / (2 * a);
	float r2 = (float)(-Math.sqrt(discriminant) - b) / (2 * a);
	System.out.println("The equation has two roots " + r1 + " and " + r2);
} else if(discriminant < 0) {
	System.out.println("The equation has no real roots.");
} else {
  	float r = (float)(Math.sqrt(discriminant) - b) / (2 * a);
	System.out.printf("The equation has one root " + r);
}
```

- 3.2

```java
Scanner sc = new Scanner(System.in);
int number1 = (int)(System.currentTimeMillis() % 10);
int number2 = (int)(System.currentTimeMillis() / 7 % 10);
int number3 = (int)(System.currentTimeMillis() / 12 % 10);

System.out.print("What is " + number1 + " + " + number2 + " + " + number3 + "? ");
int answer = sc.nextInt();
System.out.println(number1 + " + " + number2 + " + " + number3 + " = " + answer 
                   + " = " + (number1 + number2 + number3 == answer));
```

- 3.3

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter a, b, c, d, e, f: ");
double a = sc.nextDouble();
double b = sc.nextDouble();
double c = sc.nextDouble();
double d = sc.nextDouble();
double e = sc.nextDouble();
double f = sc.nextDouble();

double n = a * d - b * c;

if(n == 0) {
	System.out.println("The equation has no solution.");
} else {
	double x = (e * d - b * f) / n;
	double y = (a * f - e * c) / n;
	System.out.println("x is " + x + " and y is " + y);
}
```

- 3.4

```java
int month = (int)(System.currentTimeMillis() % 12);
switch(month) {
    case 0:
        System.out.println("Jan.");
        break;
    case 1:
        System.out.println("Feb.");
        break;
    case 2:
        System.out.println("Mar.");
        break;
    case 3:
        System.out.println("Apr.");
        break;
    case 4:
        System.out.println("May");
        break;
    case 5:
        System.out.println("Jun.");
        break;
    case 6:
        System.out.println("Jul.");
        break;
    case 7:
        System.out.println("Aug.");
        break;
    case 8:
        System.out.println("Sep.");
        break;
    case 9:
        System.out.println("Oct.");
        break;
    case 10:
        System.out.println("Nov.");
        break;
    case 11:
        System.out.println("Dec.");
        break;
}
```

- 3.5

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter today's day: ");
int week = sc.nextInt();
System.out.print("Enter the number of days elapsed since today: ");
int numberOfDay = sc.nextInt();

numberOfDay %= 7;
numberOfDay += week;

switch(week) {
    case 0:
        System.out.print("Today is Sunday and ");
        break;
    case 1:
        System.out.print("Today is Monday and ");
        break;
    case 2:
        System.out.print("Today is Tuesday and ");
        break;
    case 3:
        System.out.print("Today is Wednesday and ");
        break;
    case 4:
        System.out.print("Today is Thursday and ");
        break;
    case 5:
        System.out.print("Today is Friday and ");
        break;
    case 6:
        System.out.print("Today is Saturday and ");
        break;
}

switch(numberOfDay) {
    case 0:
        System.out.print("the future day is Sunday");
        break;
    case 1:
        System.out.print("the future day is Monday");
        break;
    case 2:
        System.out.print("the future day is Tuesday");
        break;
    case 3:
        System.out.print("the future day is Wednesday");
        break;
    case 4:
        System.out.print("the future day is Thursday");
        break;
    case 5:
        System.out.print("the future day is Friday");
        break;
    case 6:
        System.out.print("the future day is Saturday");
        break;
}
```

- 3.6

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter weight in pounds: ");
double weight = sc.nextDouble();
System.out.print("Enter feet: ");
double feet = sc.nextDouble();
System.out.print("Enter inches: ");
double height = sc.nextDouble();

final double KILOGRAMS_PER_POUND = 0.45359237;
final double METERS_PER_INCHES = 0.0254;
final int FEET_PER_INCHES = 12;

double weightInKilograms = weight * KILOGRAMS_PER_POUND;
double heightInMeters = (feet * FEET_PER_INCHES + height) * METERS_PER_INCHES;
double bmi = weightInKilograms / Math.pow(heightInMeters, 2);
System.out.println("BMI is " + bmi);

if(bmi < 18.5)
	System.out.println("Underweight");
else if(bmi < 25)
  	System.out.println("Normal");
else if(bmi < 30)
  	System.out.println("Overweight");
else
  	System.out.println("Obese");
```

- 3.7

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter an amount in double, for example 11.56: ");
double amount = sc.nextDouble();

int remainingAmount = (int)(amount * 100);

int numberOfOneDollars = remainingAmount / 100;
remainingAmount %= 100;

int numberOfQuarters = remainingAmount / 25;
remainingAmount %= 25;

int numberOfDimes = remainingAmount / 10;
remainingAmount %= 10;

int numberOfNickels = remainingAmount / 5;
remainingAmount %= 5;

int numberOfPennies = remainingAmount;

System.out.println("Your amount " + amount + " consists of");
if(numberOfOneDollars > 0) {
	System.out.print("    " + numberOfOneDollars);
	if(numberOfOneDollars == 1)
		System.out.println(" dollar");
	else
		System.out.println(" dollars");
}
if(numberOfQuarters > 0) {
	System.out.print("    " + numberOfQuarters);
	if(numberOfQuarters == 1)
		System.out.println(" quarter");
	else
		System.out.println(" quarters");
}
if(numberOfDimes > 0) {
	System.out.print("    " + numberOfDimes);
	if(numberOfDimes == 1)
		System.out.println(" dime");
	else
		System.out.println(" dimes");
}
if(numberOfNickels > 0) {
	System.out.print("    " + numberOfNickels);
	if(numberOfNickels == 1)
		System.out.println(" nickel");
	else
		System.out.println(" nickels");
}
if(numberOfPennies > 0) {
	System.out.print("    " + numberOfPennies);
	if(numberOfPennies == 1)
		System.out.println(" pennie");
	else
		System.out.println(" pennies");
}
```

- 3.8

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter three integer: ");
int number1 = sc.nextInt();
int number2 = sc.nextInt();
int number3 = sc.nextInt();

if(number1 < number2)
	if(number2 < number3)
		System.out.printf("%d - %d - %d\n", number1, number2, number3);
	else
		System.out.printf("%d - %d - %d\n", Math.min(number1, number3),
			Math.max(number1, number3), number2);
else
	if(number1 < number3)
		System.out.printf("%d - %d - %d\n", number2, number1, number3);
	else
		System.out.printf("%d - %d - %d\n", Math.min(number2, number3),
			Math.max(number2, number3), number1);
```

- 3.9

```

```

- 3.10

```java
Scanner sc = new Scanner(System.in);
int number1 = (int)(Math.random() * 100);
int number2 = (int)(Math.random() * 100);

if(number1 < number2) {
	int temp = number1;
 	number1 = number2;
  	number2 = temp;
}

System.out.print("What is " + number1 + " - " + number2 + "? ");
int answer = sc.nextInt();

if((number1 - number2) == answer)
	System.out.println("You are correct!");
else {
  	System.out.println("Your answer is wrong.");
  	System.out.println(number1 + " - " + number2 + " should be "
                       + (number1 - number2));
}
```

- 3.11

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter month and year: ");
int month = sc.nextInt();
int year = sc.nextInt();

switch(month) {
    case 1:
        System.out.printf("Jan. %d had 31 days.", year);
        break;
    case 2:
        if((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0))
            System.out.printf("Feb. %d has 29 days", year);
        else
            System.out.printf("Feb. %d has 28 days", year);
        break;
    case 3:
        System.out.printf("Mar. %d had 31 days.", year);
        break;
    case 4:
        System.out.printf("Apr. %d had 30 days.", year);
        break;
    case 5:
        System.out.printf("May %d had 31 days.", year);
        break;
    case 6:
        System.out.printf("Jun. %d had 30 days.", year);
        break;
    case 7:
        System.out.printf("Jul. %d had 31 days.", year);
        break;
    case 8:
        System.out.printf("Aug. %d had 31 days.", year);
        break;
    case 9:
        System.out.printf("Sep. %d had 30 days.", year);
        break;
    case 10:
        System.out.printf("Oct. %d had 31 days.", year);
        break;
    case 11:
        System.out.printf("Nov. %d had 30 days.", year);
        break;
    case 12:
        System.out.printf("Dec. %d had 31 days.", year);
        break;
}
```

- 3.12

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter a three-digit integer: ");
int number = sc.nextInt();

if((number / 100) == (number % 10))
	System.out.println(number + " is a palindrome");
else
	System.out.println(number + " is not a palindrome");
```

- 3.13

```java
Scanner sc = new Scanner(System.in);
System.out.print("(0-single filer, 1-married jointly or qualifying widow(er)," 
        + " 2-married separately, 3-head of household) Enter the filing status: ");
int status = sc.nextInt();

System.out.print("Enter the taxable income: ");
double income = sc.nextDouble();

double tax = 0;

switch(status){
	case 0:
		if(income <= 8350)
			tax = income * 0.10;
		else if(income <= 33950)
			tax = 8350 * 0.1 + (income - 8350) * 0.15;
		else if(income <= 82250)
			tax = 8350 * 0.1 + (33950 - 8350) * 0.15 + (income - 33950) * 0.25;
		else if(income <= 171550)
			tax = 8350 * 0.1 + (33950 - 8350) * 0.15 + (82250 - 33950) * 0.25 +
				(income - 82250) * 0.28;
		else if(income <= 372950)
			tax = 8350 * 0.1 + (33950 - 8350) * 0.15 + (82250 - 33950) * 0.25 +
				(171550 - 82250) * 0.28 + (income - 171550) * 0.33;
		else
			tax = 8350 * 0.1 + (33950 - 8350) * 0.15 + (82250 - 33950) * 0.25 + 
      	(171550 - 82250) * 0.28 + (372950 - 171550) * 0.33 + (income - 372950) 
		break;
    case 1:
        if(income <= 16700)
            tax = income * 0.10;
        else if(income <= 67900)
            tax = 16700 * 0.1 + (income - 16700) * 0.15;
        else if(income <= 137050)
            tax = 16700 * 0.1 + (67900 - 16700) * 0.15 + (income - 67900) * 0.25;
        else if(income <= 208850)
            tax = 16700 * 0.1 + (67900 - 16700) * 0.15 + (137050 - 33950) * 0.25 +
            (income - 137050) * 0.28;
        else if(income <= 372950)
            tax = 16700 * 0.1 + (67900 - 16700) * 0.15 + (137050 - 33950) * 0.25 +
            (208850 - 137050) * 0.28 + (income - 208850) * 0.33;
        else
            tax = 16700 * 0.1 + (67900 - 16700) * 0.15 + (137050 - 33950) * 0.25 +
            (208850 - 137050) * 0.28 + (372950 - 208850) * 0.33 + (income - 372950) 
            * 0.35;
        break;
    case 2:
        if(income <= 8350)
            tax = income * 0.10;
        else if(income <= 33950)
            tax = 8350 * 0.1 + (income - 8350) * 0.15;
        else if(income <= 68525)
            tax = 8350 * 0.1 + (33950 - 8350) * 0.15 + (income - 33950) * 0.25;
        else if(income <= 104425)
            tax = 8350 * 0.1 + (33950 - 8350) * 0.15 + (68525 - 33950) * 0.25 +
            (income - 68525) * 0.28;
        else if(income <= 186475)
            tax = 8350 * 0.1 + (33950 - 8350) * 0.15 + (68525 - 33950) * 0.25 +
            (104425 - 68525) * 0.28 + (income - 104425) * 0.33;
        else
            tax = 8350 * 0.1 + (33950 - 8350) * 0.15 + (68525 - 33950) * 0.25 +
            (104425 - 68525) * 0.28 + (186475 - 171550) * 0.33 + (income - 186475) 
            * 0.35;
        break;
    case 3:
        if(income <= 11950)
            tax = income * 0.10;
        else if(income <= 45500)
            tax = 11950 * 0.1 + (income - 11950) * 0.15;
        else if(income <= 117450)
            tax = 11950 * 0.1 + (45500 - 11950) * 0.15 + (income - 45500) * 0.25;
        else if(income <= 190200)
            tax = 11950 * 0.1 + (45500 - 11950) * 0.15 + (117450 - 45500) * 0.25 +
            (income - 117450) * 0.28;
        else if(income <= 372950)
            tax = 11950 * 0.1 + (45500 - 11950) * 0.15 + (117450 - 45500) * 0.25 +
            (190200 - 117450) * 0.28 + (income - 190200) * 0.33;
        else
            tax = 11950 * 0.1 + (45500 - 11950) * 0.15 + (117450 - 45500) * 0.25 +
            (190200 - 117450) * 0.28 + (372950 - 190200) * 0.33 + (income - 372950) 
            * 0.35;
        break;
    default:
        System.out.println("Error: invalid status.");
        System.exit(1);
}

System.out.println("Tax is " + (int)(tax * 100) / 100.0);
```

- 3.14

```java
Scanner sc = new Scanner(System.in);
System.out.print("(0-tail, 1-head) Enter the flip of coin: ");
int guess = sc.nextInt();

if(guess == (int)(Math.random() * 100) % 2)
	System.out.println("Your guess is correct");
else
	System.out.println("Your guess is incorrect");
```

- 3.15

```java

```

- 3.16

```java

```

- 3.17

```java
Scanner sc = new Scanner(System.in);
int computer = (int)(Math.random() * 100) % 3;
System.out.print("scissor(0), rock(1), paper(2): ");
int user = sc.nextInt();

switch(user) {
    case 0:
        switch (computer) {
            case 0:
                System.out.println("The computer is scissor. You are scissor." + 
                                   " It is a draw");
                break;
            case 1:
                System.out.println("The computer is rock. You are scissor." + 
                                   " The computer won");
                break;
            case 2:
                System.out.println("The computer is paper. You are scissor." + 
                                   " You won");
                break;
        }
        break;
    case 1:
        switch (computer) {
            case 0:
                System.out.println("The computer is scissor. You are rock." + 
                                   " You won");
                break;
            case 1:
                System.out.println("The computer is rock. You are rock." + 
                                   " It is a draw");
                break;
            case 2:
                System.out.println("The computer is paper. You are rock." + 
                                   " The computer won");
                break;
        }
        break;
    case 2:
        switch (computer) {
            case 0:
                System.out.println("The computer is scissor. You are paper." + 
                                   " The computer won");
                break;
            case 1:
                System.out.println("The computer is rock. You are paper. You won");
                break;
            case 2:
                System.out.println("The computer is paper. You are paper." + 
                                   " It is a draw");
                break;
        }
        break;
}
```

- 3.18

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter the weight of the package: ");
double weight = sc.nextDouble();

if(weight > 0 && weight <= 1)
	System.out.println("The cost of shipping is " + 3.5);
else if(weight <= 3)
	System.out.println("The cost of shipping is " + 5.5);
else if(weight <= 10)
	System.out.println("The cost of shipping is " + 8.5);
else if(weight <= 20)
	System.out.println("The cost of shipping is " + 10.5);
else
	System.out.println("The package cannot be shipped");
```

- 3.19

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter three edges for a triangle: ");
double side1 = sc.nextDouble();
double side2 = sc.nextDouble();
double side3 = sc.nextDouble();

if(side1 + side2 > side3 && side1 + side3 > side2 && side2 + side3 > side1)
	System.out.println("The triangle's perimeter is " + (side1 + side2 + side3));
else
	System.out.println("Your input is invalid");
```

- 3.20

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter the temperature in Fahrenheit between -58℉ and 41℉: ");
double temperature = sc.nextDouble();
System.out.print("Enter the wind speed(>=2) in miles per hour: ");
double windSpeed = sc.nextDouble();

boolean isTemperature = temperature + 58 < 0 || temperature - 41 > 0;
boolean isWindSpeed = windSpeed < 2;

if(isTemperature && isWindSpeed)
	System.out.println("the temperature and wind speed is invalid");
else if(isTemperature)
	System.out.println("the temperature is invalid");
else if(isWindSpeed)
	System.out.println("the wind speed is invalid");
else
	System.out.printf("The wind chill index is %.5f",
		35.74 + 0.6215 * temperature - 35.75 * Math.pow(windSpeed, 0.16) +
		0.4275 * temperature * Math.pow(windSpeed, 0.16));
```

- 3.21

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter year (e.g., 2012): ");
int year = sc.nextInt();
System.out.print("Enter month (1-12): ");
int month = sc.nextInt();
System.out.print("Enter day of the month (1-31): ");
int dayOfMonth = sc.nextInt();

if(month == 1 || month == 2) {
	month += 12;
	year--;
}

int h = (dayOfMonth + (26 * (month + 1)) / 10 + year % 100 + year % 100 / 4
		+ year / 100 / 4 + 5 * year / 100) % 7;
switch (h) {
    case 0:
        System.out.println("Day of the week is Saturday");
        break;
    case 1:
        System.out.println("Day of the week is Sunday");
        break;
    case 2:
        System.out.println("Day of the week is Monday");
        break;
    case 3:
        System.out.println("Day of the week is Tuesday");
        break;
    case 4:
        System.out.println("Day of the week is Wednesday");
        break;
    case 5:
        System.out.println("Day of the week is Thursday");
        break;
    case 6:
        System.out.println("Day of the week is Friday");
        break;
}
```

- 3.22

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter a point with two coordinates: ");
double x = sc.nextDouble();
double y = sc.nextDouble();

double distance = Math.sqrt(Math.pow(x, 2) + Math.pow(y, 2));

if(distance - 10 > 0)
	System.out.printf("Point (%.1f, %.1f) is not in the circle.", x, y);
else
	System.out.printf("Point (%.1f, %.1f) is in the circle.", x, y);
```

- 3.23

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter a point with two coordinates: ");
double x = sc.nextDouble();
double y = sc.nextDouble();

boolean flag = Math.abs(x) - 5 > 0 || Math.abs(y) - 2.5 > 0;

if(flag)
	System.out.printf("Point (%.1f, %.1f) is not in the rectangle", x, y);
else
	System.out.printf("Point (%.1f, %.1f) is in the rectangle", x, y);
```

- 3.24

```java
int rank = (int)(Math.random() * 100) % 13;
int suit = (int)(Math.random() * 100) % 4;

switch (rank) {
    case 0:
        switch (suit) {
            case 0:
                System.out.println("The card you picked is Ace of Clubs");
                break;
            case 1:
                System.out.println("The card you picked is Ace of Diamonds");
                break;
            case 2:
                System.out.println("The card you picked is Ace of Hearts");
                break;
            case 3:
                System.out.println("The card you picked is Ace of Spades");
                break;
        }
        break;
    case 10:
        switch (suit) {
            case 0:
                System.out.println("The card you picked is Jack of Clubs");
                break;
            case 1:
                System.out.println("The card you picked is Jack of Diamonds");
                break;
            case 2:
                System.out.println("The card you picked is Jack of Hearts");
                break;
            case 3:
                System.out.println("The card you picked is Jack of Spades");
                break;
        }
        break;
    case 11:
        switch (suit) {
            case 0:
                System.out.println("The card you picked is Queen of Clubs");
                break;
            case 1:
                System.out.println("The card you picked is Queen of Diamonds");
                break;
            case 2:
                System.out.println("The card you picked is Queen of Hearts");
                break;
            case 3:
                System.out.println("The card you picked is Queen of Spades");
                break;
        }
        break;
    case 12:
        switch (suit) {
            case 0:
                System.out.println("The card you picked is King of Clubs");
                break;
            case 1:
                System.out.println("The card you picked is King of Diamonds");
                break;
            case 2:
                System.out.println("The card you picked is King of Hearts");
                break;
            case 3:
                System.out.println("The card you picked is King of Spades");
                break;
        }
        break;
    default:
        switch (suit) {
            case 0:
                System.out.printf("The card you picked is %d of Clubs", rank + 1);
                break;
            case 1:
                System.out.printf("The card you picked is %d of Diamonds", rank+1);
                break;
            case 2:
                System.out.printf("The card you picked is %d of Hearts", rank + 1);
                break;
            case 3:
                System.out.printf("The card you picked is %d of Spades", rank + 1);
                break;
        }
}
```

- 3.25

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter x1, y1, x2, y2, x3, y3, x4, y4: ");
double x1 = sc.nextDouble();
double y1 = sc.nextDouble();
double x2 = sc.nextDouble();
double y2 = sc.nextDouble();
double x3 = sc.nextDouble();
double y3 = sc.nextDouble();
double x4 = sc.nextDouble();
double y4 = sc.nextDouble();

double a = y1 - y2;
double b = x2 - x1;
double c = y3 - y4;
double d = x4 - x3;
double e = (y1 - y2) * x1 - (x1 - x2) * y1;
double f = (y3 - y4) * x3 - (x3 - x4) * y3;

double n = a * d - b * c;
if (n != 0)
	System.out.printf("The intersecting point is at (%.4f, %.4f)",
		(e * d - b * f) / n, (a * f - e * c) / n);
else
	System.out.print("The two lines are parallel");
```

- 3.26

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter an integer: ");
int number = sc.nextInt();
System.out.println("Is " + number + " divisible by 5 and 6? " +
		(number % 5 == 0 && number % 6 ==0));
System.out.println("Is " + number + " divisible by 5 or 6? " +
		(number % 5 == 0 || number % 6 == 0));
System.out.println("Is " + number + " divisible by 5 or 6, but not both? " +
		(number % 5 == 0 ^ number % 6 == 0));
```

- 3.27

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter a point's x- and y-coordinates: ");
double x = sc.nextDouble();
double y = sc.nextDouble();

boolean flag1 = x < 0 || y < 0 || x > 200 || y > 100;
boolean flag2 = x <= 200 - 2 * y;

if(!flag1 && flag2)
	System.out.println("The point is in the triangle");
else
	System.out.println("The point is not in the triangle");
```

- 3.28

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter r1's center x-, y-coordinates, width, and height: ");
double r1x = sc.nextDouble();
double r1y = sc.nextDouble();
double r1Width = sc.nextDouble();
double r1Height = sc.nextDouble();

System.out.print("Enter r2's center x-, y-coordinates, width, and height: ");
double r2x = sc.nextDouble();
double r2y = sc.nextDouble();
double r2Width = sc.nextDouble();
double r2Height = sc.nextDouble();

double xDistance = Math.abs(Math.abs(r1x) - Math.abs(r2x));
double yDistance = Math.abs(Math.abs(r1y) - Math.abs(r2y));

boolean isOverlap = yDistance > (r1Height + r2Height) / 2 || 
  xDistance> (r1Width + r2Width) / 2;

boolean isInside = yDistance <= r1Height / 2 && xDistance <= r1Width / 2;

if(isOverlap)
		System.out.println("r2 does not overlap r1");
else
	if(isInside)
		System.out.println("r2 is inside r1");
	else
		System.out.println("r2 overlaps r1");
```

- 3.29

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter circle1's center x-, y-coordinates, and radius: ");
double r1x = sc.nextDouble();
double r1y = sc.nextDouble();
double radius1 = sc.nextDouble();

System.out.print("Enter circle2's center x-, y-coordinates, and radius: ");
double r2x = sc.nextDouble();
double r2y = sc.nextDouble();
double radius2 = sc.nextDouble();

double distance = Math.sqrt(Math.pow(r1x - r2x, 2) + Math.pow(r1y - r2y, 2));

boolean isInside = distance <= Math.abs(radius1 - radius2);
boolean isOverlap = distance <= radius1 + radius2;

if(isInside)
	System.out.println("circle2 is inside circle1");
else
	if(isOverlap)
		System.out.println("circle2 overlaps circle1");
	else
		System.out.println("circle2 does not overlap circle1");
```

- 3.30

```java
System.out.print("Enter the time zone offset to GMT: ");
Scanner sc = new Scanner(System.in);
int offset = sc.nextInt();

long totalMilliseconds = System.currentTimeMillis();

long totalSeconds = totalMilliseconds / 1000;
long currentSecond = totalSeconds % 60;

long totalMinutes = totalSeconds / 60;
long currentMinute = totalMinutes % 60;

long totalHours = totalMinutes / 60;
long currentHour = totalHours % 24 + offset;

if(currentHour > 24)
currentHour -= 24;
else if(currentHour < 0)
currentHour += 24;

if(currentHour > 12) 
	System.out.println("Current time is " + (currentHour - 12) + ":" + 
                       currentMinute + ":" + currentSecond + " PM");
else
	System.out.println("Current time is " + currentHour + ":" + 
                       currentMinute + ":" + currentSecond + " AM");
```

- 3.31

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter the exchange rate from dollars to RMB: ");
double exchangeRate = sc.nextDouble();
System.out.print("Enter 0 to convert dollars to RMB and 1 vice versa: ");
int flag = sc.nextInt();
double amount = 0;

switch (flag) {
    case 0:
        System.out.print("Enter the dollar amount: ");
        amount = sc.nextDouble();
        System.out.printf("$%.1f is %.1f yuan.", amount, amount * exchangeRate);
        break;
    case 1:
        System.out.print("Enter the RMB amount: ");
        amount = sc.nextDouble();
        System.out.printf("%.1f yuan is $%.2f", amount, amount / exchangeRate);
        break;
    default:
        System.out.println("Incorrect input.");
}
```

- 3.32

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter three points for p0, p1, and p2: ");
double p0x = sc.nextDouble();
double p0y = sc.nextDouble();
double p1x = sc.nextDouble();
double p1y = sc.nextDouble();
double p2x = sc.nextDouble();
double p2y = sc.nextDouble();

double condition = (p1x - p0x) * (p2y - p0y) - (p2x - p0x) * (p1y - p0y);

if (condition > 0)
    System.out.printf("(%.1f, %.1f) is on the left side of the line from (%.1f, " + 
                      "%.1f) to (%.1f, %.1f)", p2x, p2y, p0x, p0y, p1x, p1y);
else if (condition < 0)
    System.out.printf("(%.1f, %.1f) is on the right side of the line from (%.1f, " +
                      " %.1f) to (%.1f, %.1f)", p2x, p2y, p0x, p0y, p1x, p1y);
else
    System.out.printf("(%.1f, %.1f) is on the line from (%.1f, %.1f) to (%.1f, " + 
                      "%.1f)", p2x, p2y, p0x, p0y, p1x, p1y);
```

- 3.33

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter weight and price for package 1: ");
double package1Weight = sc.nextDouble();
double package1Price = sc.nextDouble();
System.out.print("Enter weight and price for package 2: ");
double package2Weight = sc.nextDouble();
double package2Price = sc.nextDouble();

double package1Cost = package1Price / package1Weight;
double package2Cost = package2Price / package2Weight;

if (package1Cost - package2Cost > 0)
    System.out.println("Package 2 has a better price");
else if (package1Cost - package2Cost < 0)
    System.out.println("Package 1 has a better price");
else
    System.out.println("Two package has the same price");
```

- 3.34

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter three points for p0, p1, and p2: ");
double p0x = sc.nextDouble();
double p0y = sc.nextDouble();
double p1x = sc.nextDouble();
double p1y = sc.nextDouble();
double p2x = sc.nextDouble();
double p2y = sc.nextDouble();

double condition = (p1x - p0x) * (p2y - p0y) - (p2x - p0x) * (p1y - p0y);
boolean isOnLineSegment = ((p2x > p0x) && (p2x < p1x)) || ((p2x < p0x) && (p2x > p1x));

if ((condition == 0) && isOnLineSegment)
    System.out.printf("(%.1f, %.1f) is on the line segment from (%.1f, %.1f)" + 
                      " to (%.1f, %.1f)", p2x, p2y, p0x, p0y, p1x, p1y);
else
    System.out.printf("(%.1f, %.1f) is not on the line segment from (%.1f, %.1f)" + 
                      " to (%.1f, %.1f)", p2x, p2y, p0x, p0y, p1x, p1y);
```

