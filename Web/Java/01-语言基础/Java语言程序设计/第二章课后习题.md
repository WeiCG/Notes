# 第二章课后习题

> 以下答案全部省略main函数和导入的默认包

- 2.1

```java
System.out.print("Enter a degree in Celsius: ");
Scanner sc = new Scanner(System.in);
double celsius = sc.nextInt();

double fahrenheit = (9.0 / 5) * celsius + 32;
System.out.println(celsius + " Celsius is " + fahrenheit + " Fahrenheit.");
```

- 2.2

```java
System.out.print("Enter the radius and length of a cylinder: ");
Scanner sc = new Scanner(System.in);
double radius = sc.nextDouble();
double length = sc.nextDouble();

double area = radius * radius * Math.PI;
double volume = area * length;
System.out.println("The area is " + area);
System.out.println("The volume is " + volume);
```

- 2.3

```java
System.out.print("Enter a value for feet: ");
Scanner sc = new Scanner(System.in);
double feet = sc.nextDouble();

double meter = feet * 0.305;
System.out.println(feet + " feet is " + meter + " meters.");
```

- 2.4

```java
System.out.print("Enter a number in pounds: ");
Scanner sc = new Scanner(System.in);
double pounds = sc.nextDouble();

double kilograms = pounds * 0.454;
System.out.println(pounds + " pounds is " + kilograms + " kilograms.");
```

- 2.5

```java
System.out.print("Enter the subtotal and a gratuity rate: ");
Scanner sc = new Scanner(System.in);
double subtotal = sc.nextDouble();
double rate = sc.nextDouble();

double gratuity = subtotal * rate * 0.01;
double total = subtotal + gratuity;
System.out.println("The gratuity is $" + gratuity + " and total is $" + total);
```

- 2.6

```java
System.out.print("Enter a integer between 0 and 1000: ");
Scanner sc = new Scanner(System.in);
int number = sc.nextInt();

int hundreds = number / 100;
int ten = (number % 100) / 10;
int unit = (number % 100) % 10;

System.out.println("The sum of the digits is " + (hundreds + ten + unit));
```

- 2.7

```java
System.out.print("Enter the number of minutes: ");
Scanner sc = new Scanner(System.in);
int minutes = sc.nextInt();

int hours = minutes / 60;
int days = hours / 24;
int years = days / 365;
days %= 365;

System.out.println(minutes + " minutes is approximately " + years + " years and " + 
                   days + " days.");
```

- 2.8

```java
System.out.print("Enter the time zone offset to GMT: ");
Scanner sc = new Scanner(System.in);
int offset = sc.nextInt();

long totalMilliseconds = System.currentTimeMillis();

long totalSeconds = totalMilliseconds / 1000;
long currentSecond = totalSeconds % 60;

long totalMinutes = totalSeconds / 60;
long currentMinute = totalMinutes % 60;

long totalHours = totalMinutes / 60;
long currentHour = totalHours % 24 + offset;

if(currentHour > 24)
	currentHour %= 24;
else if(currentHour < 0)
	currentHour += 24;

System.out.println("Current time is " + currentHour + ":" + currentMinute 
                   + ":" + currentSecond + " GMT");
```

- 2.9

```java
System.out.print("Enter v0, v1 and t: ");
Scanner sc = new Scanner(System.in);
double v0 = sc.nextDouble();
double v1 = sc.nextDouble();
double t = sc.nextDouble();

double a = (v1 - v0) / t;

System.out.printf("The average acceleration is %.4f", a);
```

- 2.10

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter the amount of water in kilograms: ");
double AmountOfWater = sc.nextDouble();
System.out.print("Enter the initial temperature: ");
double initialTemperature = sc.nextDouble();
System.out.print("Enter the final temperature: ");
double finalTemperature = sc.nextDouble();

double energy = AmountOfWater * (finalTemperature - initialTemperature) * 4184;

System.out.printf("The energy needed is %.1f", energy);
```

- 2.11

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter the number of year: ");
int year = sc.nextInt();

long seconds = year * 365 * 24 * 60 * 60;
long population = 312032486 + seconds / 7 - seconds / 13 + seconds / 45;

System.out.println("The population in " + year + " years is " + population);
```

- 2.12

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter speed and acceleration: ");
double speed = sc.nextDouble();
double acceleration = sc.nextDouble();

double length = speed * speed / (2 * acceleration);

System.out.printf("The minimum runway length for this airplane is %.3f", length);
```

- 2.13

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter the monthly saving amount: ");
double amount = sc.nextDouble();

double account = amount;
account = amount * (1 + 0.00417); // first
account = (amount + account) * (1 + 0.00417);  // second
account = (amount + account) * (1 + 0.00417);  // third
account = (amount + account) * (1 + 0.00417);  // fourth
account = (amount + account) * (1 + 0.00417);  // fifth
account = (amount + account) * (1 + 0.00417);  // sixth
System.out.printf("After the sixth month, the account value is $%.2f", account);
```

- 2.14

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter weight in pounds: ");
double weight = sc.nextDouble();
System.out.print("Enter height in inches: ");
double height = sc.nextDouble();

double BMI = (weight * 0.45359237) / Math.pow(height * 0.0254, 2);

System.out.printf("BMI is %.4f", BMI);
```

- 2.15

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter x1 and y1: ");
double x1 = sc.nextDouble();
double y1 = sc.nextDouble();
System.out.print("Enter x2 and y2: ");
double x2 = sc.nextDouble();
double y2 = sc.nextDouble();

double distance = Math.sqrt(Math.pow(x2 - x1, 2) + Math.pow(y2 - y1, 2));

System.out.println("The distance between the two points is " + distance);
```

- 2.16

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter the side: ");
double side = sc.nextDouble();

double area = 3 * Math.sqrt(3) / 2 * Math.pow(side, 2);

System.out.printf("The area of the hexagon is %.4f", area);
```

- 2.17

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter the temperature in Fahrenheit between -58℉ and 41℉: ");
double temperature = sc.nextDouble();
System.out.print("Enter the wind speed(>=2) in miles per hour: ");
int windSpeed = sc.nextInt();

double windChillIndex = 35.74 + 0.6215 * temperature - 35.75 * 
  Math.pow(windSpeed, 0.16) + 0.4275 * temperature * Math.pow(windSpeed, 0.16);

System.out.printf("The wind chill index is %.5f", windChillIndex);
```

- 2.18

```java
System.out.printf("%-5c%-5c%-10s\n", 'a', 'b', "pow(a, b)");
System.out.printf("%-5c%-5c%-10d\n", '1', '2', (int)Math.pow(1, 2));
System.out.printf("%-5c%-5c%-10d\n", '2', '3', (int)Math.pow(2, 3));
System.out.printf("%-5c%-5c%-10d\n", '3', '4', (int)Math.pow(3, 4));
System.out.printf("%-5c%-5c%-10d\n", '4', '5', (int)Math.pow(4, 5));
System.out.printf("%-5c%-5c%-10d\n", '5', '6', (int)Math.pow(5, 6));
```

- 2.19

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter three points for a triangle: ");
double x1 = sc.nextDouble();
double y1 = sc.nextDouble();
double x2 = sc.nextDouble();
double y2 = sc.nextDouble();
double x3 = sc.nextDouble();
double y3 = sc.nextDouble();

double side1 = Math.sqrt(Math.pow(x2 - x1, 2) + Math.pow(y2 - y1, 2));
double side2 = Math.sqrt(Math.pow(x3 - x2, 2) + Math.pow(y3 - y2, 2));
double side3 = Math.sqrt(Math.pow(x3 - x1, 2) + Math.pow(y3 - y1, 2));

double s = (side1 + side2 + side3) / 2;
double area = Math.sqrt(s * (s - side1) * (s - side2) * (s - side3));
System.out.printf("The area of the triangle is %.1f", area);
```

- 2.20

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter balance and interest rate (e.g., 3 for 3%): ");
double balance = sc.nextDouble();
double annualInterestRate = sc.nextDouble();

double interest = balance * (annualInterestRate / 1200);

System.out.printf("The area of the triangle is %.5f", interest);
```

- 2.21

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter investment amount: ");
double investmentAmount = sc.nextDouble();
System.out.print("Enter annual interest rate in percentage: ");
double annualInterestRate = sc.nextDouble();
System.out.printf("Enter number of years: ");
int numberOfYears = sc.nextInt();

double futureInvestmentValue = investmentAmount *
Math.pow(1 + annualInterestRate / 1200, numberOfYears * 12);

System.out.printf("The area of the triangle is $%.2f", futureInvestmentValue);
```

- 2.22

```java
Scanner sc = new Scanner(System.in);
System.out.print("Enter a amount in integer, for example 1156: ");
int amount = sc.nextInt();

int cents = amount % 100;
int dollars = (int)(amount / 100);
System.out.println("Your amount " + amount + " consists of");
System.out.printf("\t%d dollars\n\t%d cents", dollars, cents);
```

- 2.23

```java
Scanner sc = new Scanner(System.in);
System.out.printf("Enter the driving distance: ");
double distance = sc.nextDouble();
System.out.printf("Enter miles per gallon: ");
double miles = sc.nextDouble();
System.out.printf("Enter price per gallon: ");
double price = sc.nextDouble();

double cost = distance / miles * price;

System.out.printf("The cost of driving is $%.2f", cost);
```

